import WorkspacePlaceholder from '@/components/WorkspacePlaceholder';

export default function Championship() {
  return <WorkspacePlaceholder title="Championship Intelligence" description="Driver standings, constructor standings, expected points, title probability and momentum analysis will live here." />;
}
