import WorkspacePlaceholder from '@/components/WorkspacePlaceholder';

export default function Predictor() {
  return <WorkspacePlaceholder title="Race Predictor" description="Winner probability, podium probability, confidence intervals and Monte Carlo distributions will connect to the FastAPI simulation endpoint." />;
}
