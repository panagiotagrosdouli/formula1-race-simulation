import WorkspacePlaceholder from '@/components/WorkspacePlaceholder';

export default function Reports() {
  return <WorkspacePlaceholder title="Engineering Reports" description="PDF export, model assumptions, telemetry snapshots, recommendations and limitations will be generated from this workspace." />;
}
