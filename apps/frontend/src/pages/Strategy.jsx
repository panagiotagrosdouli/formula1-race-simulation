import WorkspacePlaceholder from '@/components/WorkspacePlaceholder';

export default function Strategy() {
  return <WorkspacePlaceholder title="Strategy Lab" description="Pit windows, tyre degradation, fuel model, undercut and overcut analysis will connect to the strategy preview backend." />;
}
