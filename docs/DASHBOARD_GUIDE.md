# Dashboard Guide

Run:

```bash
streamlit run f1sim/dashboard/app.py
```

The prototype dashboard loads a YAML experiment, runs the race engine, displays lap-time and position charts and can execute Monte Carlo runs. Future work should add tyre-stint timelines, pit-window plots, strategy probability panels and report exports.
