# Evaluation Protocol

Use deterministic seeds, versioned YAML configs, public-data citations and held-out race comparisons when data is available. Report total race time, position, pit count, stint lengths, degradation rate, traffic loss, pit loss, undercut/overcut deltas, confidence intervals and target-driver win probability.
