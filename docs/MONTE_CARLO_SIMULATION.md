# Monte Carlo Simulation

`f1sim/simulation/monte_carlo.py` repeats seeded race simulations to estimate expected race time, confidence intervals and risk profiles. It supports comparing a leader against a target driver when the target is configured.
