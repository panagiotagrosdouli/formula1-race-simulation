# Tyre Model

`f1sim/models/tyres.py` defines soft, medium, hard, intermediate and wet compounds with base pace delta, wear rate, cliff behaviour, track-temperature sensitivity and wetness sensitivity. Values are configurable modelling assumptions, not official team data.
